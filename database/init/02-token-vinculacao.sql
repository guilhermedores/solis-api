-- =============================================================================
-- TABELA DE TOKENS DE VINCULAÇÃO
-- REMOVIDA - Tokens agora são JWT e não são armazenados no banco
-- Os tokens são gerados via /api/auth/generate-agent-token
-- e validados via AuthService.validateToken()
-- =============================================================================

-- Este script foi esvaziado pois não armazenamos mais tokens no banco
-- Os tokens JWT contêm empresaId, tenantId e são validados em tempo real

-- =============================================================================
-- FIM DO SCRIPT
-- =============================================================================

